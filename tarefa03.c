#include <stdio.h>
#include <stdlib.h>

int main(){
	
	int a, i, fat;
	
	scanf("%d", &a);
	for(i = 1; i < a; i++){
		fat = a * (a - 1);
	}
	printf("%d\n", fat);
	return 0;
}
//está com erro para guardar o fatorial
